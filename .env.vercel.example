# ============================================================
# Variáveis de ambiente para configurar no painel do Vercel
# Settings > Environment Variables
# ============================================================

# URL pública da sua API FastAPI (ex: Railway, Render, Fly.io)
# Obrigatória — sem ela o frontend não consegue se comunicar com o backend
NEXT_PUBLIC_API_URL=https://your-api-domain.com
